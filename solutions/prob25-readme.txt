
攻破“随机读/写”的AES CTR
回到CTR模式。从加密该文件（与ECB练习的密文相同）CTR下一个随机密钥恢复的明文（这个练习的的密钥应该是您不知道的，但是暂时不管它）。现在，写一段让你能够加密不同明文的代码，写成一个函数，如“edit(ciphertext, key, offset, newtext)”。假设“edit”功能是通过一个API调用没有透露密钥或原始明文的方式提供给攻击者调用（攻击者不能控制key参数，不知道原始明文）;攻击者拥有原始密文，并且可以控制偏置和“newtext”。
题目目标是恢复原始明文。