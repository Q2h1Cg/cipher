DSA parameter tampering
使用前面DSA签名的代码，假设客户端可以随意修改算法中的参数，就会引起攻击，攻击者可以欺骗用户接受不好的参数，比如：0 mod p 和1 mod p .
使用前面题目的参数，不过将g替换为0，进行签名操作验证签名，，这个签名也可以验证其他任意字符串的签名
将g替换为p+1，使用以下算法来对：“Hello, world”.和 "Goodbye, world"签名
 
