The CBC padding oracle
题目描述：
这是最著名的现代分组密码加密攻击。
结合填充代码和CBC代码编写两个函数。
第一个函数应该随机选择以下10个字符串之一:

…生成一个随机的AES密钥,填充后使用key在CBC模式下加密,提供调用者的密文和IV。


第二个函数
解密密文,检查它的填充,然后返回真或假取决于填充是有效的。

这两个函数（AES-CBC）作为web应用程序的部署服务端加密
第二个函数模拟服务器使用一个已加密session的过程（类似cookie）

02h 不是有效的填充。
02h 02h是有效填充,但它比出现01h概率还小。
03h 03h 03h更不太可能出现。
